spring:

  datasource:
    url: jdbc:h2:mem:climalert

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update

  mail:
    host: smtp.gmail.com
    port: 587
    username: mail
    password: contraseña
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

weather:
  api-key: api
